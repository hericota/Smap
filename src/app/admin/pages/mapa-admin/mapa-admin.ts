import { Component } from '@angular/core';
import { MapaSeparado } from '../../../components/mapa-separado/mapa-separado';

@Component({
  imports: [MapaSeparado],
  selector: 'app-mapa-admin',
  styleUrl: './mapa-admin.css',
  templateUrl: './mapa-admin.html',
})
export class MapaAdmin {}
